# Frontend (Vite) environment variables
# Vite only exposes variables prefixed with VITE_ to the browser.
VITE_MOVIE_AGENT_URL=https://movie-agent.richardpedersen3.workers.dev
VITE_BACKEND_URL=http://localhost:3000

# Backend (Node/Express) environment variables
BACKEND_PORT=3000
HOST=localhost

# Backend -> agent fallback (used if discovery is not configured / fails)
MOVIE_AGENT_URL=https://movie-agent.richardpedersen3.workers.dev
# AGENT_URL is supported for backward compatibility
# AGENT_URL=https://movie-agent.richardpedersen3.workers.dev

# Agentic Trust discovery (required if you want server-side discovery by agent name)
# AGENTIC_TRUST_DISCOVERY_URL=...

# Feedback feature (examples — required if you use feedback flows)
# CLIENT_WALLET_EOA_PRIVATE_KEY=0x...
# RPC_URL=https://rpc.sepolia.org
# REPUTATION_REGISTRY=0x...
# ERC8004_CHAIN_ID=11155111
# ENS_REGISTRY=0x00000000000C2E074eC69A0dFb2997BA6C7d2e1e

